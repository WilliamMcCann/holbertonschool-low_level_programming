/*header includes prototype for printf and
standard library*/
#include "my_functions.h"

/*start of function; function take an int
called "n" and returns void*/
void printf_number(int n)
{	
/*printf is a function from the standard
library that outputs printed text.  The
%d is a format specifier for ints.*/
	printf("%d",n);
}	
